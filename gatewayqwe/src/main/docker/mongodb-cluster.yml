version: '2'
services:
  gatewayqwe-mongodb:
    image: mongo:4.2.7
    ports:
      - '27017:27017'
    command: mongos --configdb csvr/gatewayqwe-mongodb-config --bind_ip 0.0.0.0
  gatewayqwe-mongodb-node:
    build:
      context: .
      dockerfile: mongodb/MongoDB.Dockerfile
    command: mongod --shardsvr --replSet rs1
  gatewayqwe-mongodb-config:
    image: mongo:4.2.7
    container_name: gatewayqwe-mongodb-config
    command: mongod --configsvr --dbpath /data/db --replSet csvr
